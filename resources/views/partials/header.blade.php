<header class="fixed top-0 left-0 right-0 bg-maroon px-6 py-2 flex items-center justify-between z-50 max-w-md mx-auto w-full">
    <a href="{{route('home')}}">
        <img src="{{asset('/img/solar_heart-bold-duotone.svg')}}" alt="">
    </a>

    <div class="flex items-center gap-3">
        <a href="{{route('rsvp')}}" class="bg-white text-maroon px-8 py-1 rounded-full font-semibold">
            Get Invite
        </a>
    </div>
</header>
